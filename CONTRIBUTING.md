# 贡献者开发规范

本项目已从纯前端应用演进为包含 Node.js 后端、PostgreSQL 持久化、任务队列和管理后台的完整服务。所有贡献都需要保证本地开发、Docker 部署和开源用户升级路径一致。

## 开发环境

1. 使用 `npm ci` 安装依赖，避免随意改动 `package-lock.json`。
2. 本地开发需要配置 `.env.local`，至少包含 `DATABASE_URL`、`JWT_SECRET` 和初始管理员配置。
3. 常用命令：

```bash
npm run dev          # 启动前端开发服务
npm run dev:server   # 启动后端服务
npm test             # 运行测试
npm run build        # 构建前端和后端
```

## 数据库迁移规则

数据库结构变更必须通过 `server/migrations/*.sql` 固化，禁止只在本地数据库手工执行 SQL 后提交业务代码。

- 迁移文件命名必须符合 `数字_说明.sql`，例如 `006_model_profile_environment.sql`。
- 已发布或已提交的迁移文件禁止修改。系统会把脚本内容计算 checksum 写入 `schema_migrations`，后续改动会导致启动时报 checksum 不一致。
- 新增字段、索引、约束、默认数据修复，都应追加新的迁移脚本。
- 迁移脚本应尽量幂等，优先使用 `if not exists`、安全的 `do $$ ... $$` 检查和明确约束名。
- 不要在迁移中写入个人测试数据、真实密钥、私有域名或临时调试配置。

应用启动时会自动执行 `runMigrations(db)`，Docker 镜像也会复制 `server/migrations`。因此只要脚本进入仓库，开源用户更新镜像后会自动完成数据库升级。

## 后端与配置规范

- 后端配置应来自数据库设置或环境变量，禁止在业务代码中硬编码私有域名、API Key、数据库地址等运行时配置。
- 系统设置、模型服务、用户额度等持久化配置应通过管理端和后端接口修改。
- 上游 API 请求由后端统一代理，敏感密钥保存后不回显。
- 对外接口参数必须校验，错误信息应保留足够上下文，便于管理员排查。

## 前端规范

- 前端不能把系统级配置写死在浏览器本地存储中。需要跨用户、跨设备生效的配置应走后端接口。
- 管理端页面只展示正式用户可理解的文案，避免出现“后端已支持”“调试用”等开发说明。
- 新增界面能力时同步考虑普通用户和管理员权限边界。

## 提交前检查

提交前至少执行：

```bash
npm test
npm run build
git diff --check
```

涉及数据库结构时，还需要确认新增迁移脚本已提交，并且没有修改旧迁移文件。

## 提交与 PR

- 提交信息使用简洁的中文或 conventional 风格，例如 `fix: 规范模型服务环境配置`。
- PR 说明需要写清楚：功能变更、数据库迁移、环境变量变化、测试结果和部署影响。
- UI 可见变更建议附截图；接口或数据库变更必须说明兼容性。
